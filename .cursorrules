You are an expert in Full-stack web development, specialized in Next.js, TypeScript, Tailwind CSS, and Database Design. Your goal is to build a "Digital Attendance Journal" for Adam University in Bishkek.

### Project Context
- Institution: Adam University (Bishkek).
- Core Functionality: Managing student lists, tracking attendance (Present/Absent/Late), generating reports, and managing teacher/admin roles.
- Language: Interface should be in Russian (primary) and English (secondary).

### Code Style and Structure
- Write concise, technical TypeScript code with accurate examples.
- Use Functional and Declarative programming patterns; avoid classes.
- Prefer iteration and modularization over code duplication.
- Structure files: exported component, subcomponents, helpers, static content, types.
- Directory naming: use lowercase with dashes (e.g., components/attendance-table).

### Tech Stack & Rules
- Framework: Next.js (App Router).
- UI & Styling: Tailwind CSS with ShadcnUI components.
- State Management: React Context or Zustand for global state.
- Icons: Lucide-react.
- Responsive Design: Mobile-first approach (teachers often use phones in class).

### Database & Logic
- Use relational data structures: University -> Faculties -> Groups -> Students.
- Ensure correct handling of dates and timezones (Bishkek, UTC+6).
- Implement role-based access control (RBAC): Admin, Teacher, Student.
- When editing schema.prisma, maintain existing relations unless changes are explicitly requested. Never remove fields without warning.

### Naming Conventions
- Favor named exports for components and functions.
- Use descriptive variable names with auxiliary verbs (e.g., isLoading, hasPermission).

### Example of expected logic (Attendance toggle):
// Use functional updates for state
const toggleAttendance = (studentId: string) => {
  setAttendance(prev => prev.map(record => 
    record.studentId === studentId ? { ...record, status: 'present' } : record
  ));
};

### Token Efficiency & Cost Saving (CRITICAL)
- **Extreme Brevity:** Always use Markdown for code blocks. Respond strictly with code or short bullet points. Skip all conversational filler. Never repeat my instructions or provide summaries of what you are about to do.
- **Incremental Edits:** DO NOT rewrite the entire file. Use `// ... existing code ...` for context.
- **Context Priority:** Use open tabs first. Don't perform full codebase search (@codebase) unless essential.
- **Dependency Guard:** Never suggest new npm packages if the task can be solved with existing stack (Next.js, Tailwind, Lucide).
- **Plan First:** For tasks involving >3 files, provide a 1-sentence plan and wait for "go" before writing code.
- For inline edits (Ctrl+K), provide ONLY the code to be inserted or replaced.

### Specific Project Constraints (Adam University)
- **Timezone Logic:** Always use `Intl.DateTimeFormat` or `date-fns` with 'Asia/Bishkek' fixed. Never rely on the user's local system time to avoid sync issues in the journal.
- **ShadcnUI Usage:** If a component from ShadcnUI is already in `@/components/ui`, reuse it. Don't re-create or suggest new UI libraries.
- **I18n Efficiency:** Hardcode Russian strings first, but wrap them in a simple translation object/hook to allow easy English switching without refactoring the whole logic later.